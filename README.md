# simple_volunteer_system
this project allows the manager of a volunteering system to track beneficiaries and the supports the are given along with the reason. The user can add a beneficiary, delete them, update their info, add reasons and support types and delete them if needed. the user can also filter out beneficiaries
